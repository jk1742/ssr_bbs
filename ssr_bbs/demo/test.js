// import library & font & icons & css
import _ from 'lodash';

// import
import { Logger } from '/class/Logger';

// import view block with contorller
import { Test } from 'Test';
import { TestController } from 'TestController';
// set logger //////////////////////////////////////////////////////////////////
let logger                = new Logger();
logger.status             = true;
// variable dom ////////////////////////////////////////////////////////////////
let testlayout            = new Test();
// appending body //////////////////////////////////////////////////////////////
document.body.appendChild(testlayout);
// onload //////////////////////////////////////////////////////////////////////
window.onload = function(){
  // controller ////////////////////////////////////////////////////////////////
  let test = new TestController( testlayout, {
    onload_event(e,c){
      /*
        event call here
      */
    }
  });
  // bind functions ////////////////////////////////////////////////////////////
  // test.bindfunctions()
}
////////////////////////////////////////////////////////////////////////////////
